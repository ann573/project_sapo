export interface IProduct {
    id:string,
    title: string,
    price: number,
    sku: string,
    sort_title: string,
    storage: number
}
export interface IProductBefore {
    title: string,
    price: number,
    sku: string,
    sort_title: string,
    storage:number
}